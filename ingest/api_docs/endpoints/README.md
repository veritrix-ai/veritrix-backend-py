# Endpoints

| Method | Path | Auth | Description |
|---|---|---|---|
| [`GET /health`](./health.md) | `/health` | No | Service health check |
| [`POST /v1/spans`](./ingest-spans.md) | `/v1/spans` | Yes | Ingest a batch of spans |

**Base URL:** `http://localhost:8001` (local development)
